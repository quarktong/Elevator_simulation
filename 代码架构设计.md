# 宿舍楼电梯仿真系统 —— 代码架构设计

## 1. 技术选型

| 项目 | 选型 | 理由 |
|------|------|------|
| 编程语言 | Python 3.8+ | 生态丰富，SimPy原生支持 |
| 仿真框架 | SimPy 4.x | 成熟的事件驱动离散仿真库 |
| 数据结构 | dataclass | 轻量级，无需额外依赖 |

## 2. 项目结构

```
elevator_simulation/
├── config.py               # 可配置参数（所有参数集中在此）
├── models.py               # 数据模型（Passenger, HallCall）
├── elevator.py             # 电梯 SimPy 进程（SCAN 调度核心）
├── dispatcher.py           # 调度分配器（同组互斥分配）
├── passenger_generator.py  # 乘客生成器（泊松到达）
├── simulation.py           # 仿真引擎（环境初始化）
├── statistics.py           # 统计模块（性能指标）
├── main.py                 # 入口文件
└── README.md               # 使用说明
```

## 3. 模块依赖关系

```
main.py
  └── simulation.py
        ├── config.py          （参数配置）
        ├── elevator.py        （电梯进程）
        │     └── config.py
        │     └── models.py
        ├── dispatcher.py      （调度器）
        │     └── elevator.py
        │     └── models.py
        ├── passenger_generator.py （乘客生成）
        │     └── config.py
        │     └── models.py
        │     └── dispatcher.py
        └── statistics.py      （统计）
              └── models.py
```

依赖规则：`config.py` 和 `models.py` 无外部依赖，是底层模块；`elevator.py` 依赖 config + models；`dispatcher.py` 依赖 elevator + models；`passenger_generator.py` 依赖 config + models + dispatcher；`simulation.py` 聚合所有模块。

## 4. 核心类设计

### 4.1 config.py —— 参数配置

| 类名 | 职责 | 关键属性 |
|------|------|----------|
| BuildingConfig | 建筑参数 | n_floors, n_total, n_elevators, capacity |
| TimeConfig | 时间参数 | t_travel, t_open_1, t_open, t_close, t_board, t_alight |
| TrafficConfig | 客流参数 | lambda_morning, lambda_noon, lambda_off_peak, use_prob_* |
| SimConfig | 全局配置 | building, time, traffic, sim_start, sim_end, seed |

关键方法：

- `TimeConfig.get_open_time(floor)` → `float`
- `TimeConfig.get_stop_time(floor, n_board, n_alight)` → `float`
- `TrafficConfig.get_period(sim_time)` → `TimePeriod`
- `TrafficConfig.get_lambda(period)` → `float`
- `TrafficConfig.get_use_prob(floor, period)` → `float`

### 4.2 models.py —— 数据模型

| 类名 | 职责 | 关键属性 |
|------|------|----------|
| Passenger | 乘客 | id, origin, destination, request_time, state, board_time, alight_time |
| HallCall | 厅外呼叫 | floor, direction, time, assigned_elevator, passengers |
| ElevatorSnapshot | 电梯快照 | id, position, direction, n_passengers, state |

Passenger 关键属性（计算属性）：

- `wait_time`: 上车时间 - 请求时间
- `trip_time`: 下车时间 - 上车时间
- `total_time`: 下车时间 - 请求时间

### 4.3 elevator.py —— 电梯进程（核心）

`Elevator` 类是 SimPy Process，核心方法：

| 方法 | 类型 | 说明 |
|------|------|------|
| `run()` | Process | 主循环：IDLE→等待呼叫→MOVING→逐层检查→停靠→反转/IDLE |
| `assign_call(call)` | 公共 | 调度器分配呼叫，通过 Store + Event 通知 |
| `add_car_call(floor)` | 公共 | 轿内按钮按下 |
| `is_full` / `is_idle` | 属性 | 状态查询 |
| `_handle_idle()` | 私有 | 空闲等待逻辑 |
| `_handle_moving()` | 私有 | 运行中逐层检查逻辑 |
| `_handle_stop(floor)` | 私有 | 停靠处理：开门→下客→上客→关门 |
| `_should_stop_at(floor)` | 私有 | SCAN停靠判断 |

SimPy 同步机制：

- `hall_call_store` (`simpy.Store`): 调度器放入呼叫，电梯消费
- `new_call_event` (`simpy.Event`): 新呼叫到达时唤醒空闲电梯

### 4.4 dispatcher.py —— 调度分配器

| 方法 | 说明 |
|------|------|
| `dispatch(call, passenger_dest)` | 分配呼叫：选组→互斥检查→选最优电梯 |
| `release_call(floor, direction)` | 释放已完成呼叫 |
| `_select_group(floor, dest)` | 根据奇偶性选择电梯组 |
| `_select_best_elevator(group, call)` | 组内选择：IDLE优先→同向最近→距离最近 |

### 4.5 passenger_generator.py —— 乘客生成器

| 方法 | 说明 |
|------|------|
| `run()` | SimPy Process：泊松间隔生成乘客 |
| `_generate_passenger(period)` | 按时段生成乘客（出发层+目标层+用梯概率） |
| `_sample_origin_floor(period)` | 按用梯概率采样出发层 |
| `_sample_destination_floor(period)` | 均匀采样目标层 |
| `_submit_hall_call(passenger)` | 提交呼叫给调度器 |

### 4.6 simulation.py —— 仿真引擎

| 方法 | 说明 |
|------|------|
| `setup()` | 创建SimPy环境，初始化4台电梯、调度器、生成器 |
| `run()` → `SimulationStats` | 启动所有进程，运行至sim_end，计算统计 |

## 5. 事件驱动流程

### 5.1 乘客从生成到完成的全流程

```
1. PassengerGenerator.run()
   └─ 泊松间隔到达 → _generate_passenger()
       └─ 判断用梯概率 → 创建 Passenger
           └─ _submit_hall_call()
               └─ Dispatcher.dispatch()
                   └─ _select_group() → _select_best_elevator()
                       └─ Elevator.assign_call()
                           └─ 放入 hall_call_store + 触发 new_call_event

2. Elevator.run()
   └─ _handle_idle() → 被唤醒 → 设定方向 → _handle_moving()
       └─ 逐层移动 → _should_stop_at() → _handle_stop()
           └─ 开门 → 下客(Passenger.alight_time) → 上客(Passenger.board_time) → 关门
           └─ 更新 Passenger.state = DONE

3. ElevatorSimulation.run()
   └─ env.run(until=sim_end)
       └─ SimulationStats.compute() → 输出结果
```

### 5.2 SimPy 事件时间线示例

```
时间轴（秒）:
07:40  乘客A到达8F，按下行 → 分配给E_odd_1
07:41  E_odd_1从1F出发上行
07:42  乘客B到达6F，按下行 → 分配给E_even_1
07:43  E_odd_1到达8F（运行7层×2.31s≈16s）→ 停靠开门
07:44  乘客A上车，按1F → 关门
07:45  E_odd_1下行至1F → 乘客A下车（DONE）
```

## 6. 待完善事项

以下功能在当前骨架中标记为 TODO，需后续实现：

| 编号 | 模块 | 待完善内容 | 优先级 |
|------|------|-----------|--------|
| T1 | elevator.py | `_handle_stop()` 中上客逻辑（从等待队列取乘客） | 高 |
| T2 | passenger_generator.py | 放弃等待逻辑（超时→ABANDONED） | 中 |
| T3 | dispatcher.py | 等待队列管理（两台均忙时的排队） | 中 |
| T4 | statistics.py | 按楼层/时段分组统计 | 低 |
| T5 | main.py | 命令行参数解析（argparse） | 低 |
| T6 | 新增 | 可视化输出（matplotlib图表） | 低 |
| T7 | 新增 | 多次运行取平均（Monte Carlo） | 低 |
