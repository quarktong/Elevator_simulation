# 宿舍楼电梯仿真模型

# 宿舍楼电梯运行仿真模型

## 1. 模型概述

### 1.1 建筑参数

* 总楼层数：N_floor = 17（1~17 层）
* 总人数：N_total = 1790 人
* 各层人数：各层人数基本相等，每层约 n_floor = ⌈1790 / 17⌉ ≈ 105 人
* 电梯数量：N_elevator = 4 台

  * 单数层电梯：2 台（E_odd_1, E_odd_2），服务楼层 {1, 3, 5, 7, 9, 11, 13, 15, 17}
  * 双数层电梯：2 台（E_even_1, E_even_2），服务楼层 {1, 2, 4, 6, 8, 10, 12, 14, 16}
* 电梯容量：C_max = 12 人 / 次

### 1.2 仿真目标

* 评估高峰期电梯系统的服务能力
* 关键指标：平均等待时间、最大等待时间、平均行程时间、电梯利用率、吞吐量

---

## 2. 可配置参数总表

以下所有参数均可在仿真代码中统一修改，无需改动核心逻辑：

|参数名|符号|默认值|单位|说明|
|-|-|-|-|-|
|总楼层数|N_floor|17|层|建筑总层数|
|总人数|N_total|1790|人|楼内总住宿人数|
|电梯数量|N_elevator|4|台|电梯总数|
|电梯容量|C_max|12|人 / 台|单台电梯最大载客量|
|层间运行时间|t_travel|2.31|s|单层间运行时间|
|一楼开门时间|t_open_1|10.0|s|一楼开门时间（含安全门）|
|其他层开门时间|t_open|6.8|s|非一楼开门时间|
|关门时间|t_close|4.0|s|各楼层关门时间|
|单人上车时间|t_board|1.0|s / 人|每位乘客上车时间|
|单人下车时间|t_alight|0.8|s / 人|每位乘客下车时间|
|早高峰到达率|λ_morning|0.50|人 /s|早高峰泊松到达率|
|返程高峰到达率|λ_noon|0.66|人 /s|午间返程高峰到达率|
|非高峰到达率|λ_off_peak|0.05|人 /s|非高峰期到达率|
|早高峰时段|T_morning|[07:40, 08:00]|—|早高峰起止时间|
|返程高峰时段|T_noon|[12:05, 12:20]|—|返程高峰起止时间|
|最大等待时间|T_max_wait|300|s|乘客放弃等待阈值|
|高峰用梯比例|r_peak|1/3|—|高峰期乘梯学生占比|

---

## 3. 物理参数详解

### 3.1 时间参数

|参数|符号|数值|估算依据|
|-|-|-|-|
|层间运行时间|t_travel|2.31 s|(33 - 10 - 6.8) / 7 ≈ 2.31 s / 层，基于 1F→8F（7 个区间）实测|
|一楼开门时间|t_open_1|10.0 s|文档实测值|
|其他层开门时间|t_open|6.8 s|文档实测值|
|关门时间|t_close|4.0 s|估算：一般电梯关门 3~5 秒，取中间值|
|单人上车时间|t_board|1.0 s|估算：宿舍楼学生携带书包等，取 1.0s|
|单人下车时间|t_alight|0.8 s|估算：下车略快于上车，取 0.8s|

### 3.2 电梯状态变量

对每台电梯 e ∈ {E_odd_1, E_odd_2, E_even_1, E_even_2}：

|状态变量|类型|取值范围|说明|
|-|-|-|-|
|position_e|int|{1, 2, ..., 17}|当前所在楼层|
|direction_e|enum|{UP, DOWN, IDLE}|当前运行方向|
|passengers_e|list|长度 ≤ C_max|当前载客列表|
|state_e|enum|{MOVING, DOOR_OPEN, IDLE}|运行状态|
|served_floors_e|set|单数集或双数集|可停靠楼层集合|

---

## 4. 客流模型

### 4.1 时间段划分

|时段|类型|起止时间|持续时间|到达率 λ|
|-|-|-|-|-|
|早高峰|出行高峰|07:40 - 08:00|20 min|λ_morning = 0.50 人 /s|
|午间返程|返程高峰|12:05 - 12:20|15 min|λ_noon = 0.66 人 /s|
|其他时段|非高峰|其余时间|—|λ_off_peak = 0.05 人 /s|

### 4.2 到达率估算过程

#### 早高峰（出行高峰）

* 高峰期乘梯比例 r_peak = 1/3
* 乘梯总人数 ≈ 1790 × (1/3) ≈ 597 人
* 有效乘梯楼层：5\17 层（1\4 层不乘梯），共 13 层
* 每层乘梯人数 ≈ 597 / 13 ≈ 46 人 / 层
* 高峰持续 20 min = 1200 s
* 到达率 λ_morning ≈ 597 / 1200 ≈ 0.50 人 /s

#### 返程高峰

* 乘梯总人数 ≈ 597 人（与早高峰对称）
* 高峰持续 15 min = 900 s
* 到达率 λ_noon ≈ 597 / 900 ≈ 0.66 人 /s

#### 非高峰期

* 取高峰期的 10%，λ_off_peak ≈ 0.05 人 /s

### 4.3 用梯概率模型

各层人数基本相等（每层约 105 人），用梯概率 P_use (f, period) 定义如下：

#### 出行高峰（宿舍 → 1 层）

|楼层范围|用梯概率 P_use|乘梯人数估算（每层）|
|-|-|-|
|1-4 层|0|0|
|5-6 层|0.10|≈ 10 人|
|7-10 层|0.30|≈ 32 人|
|11-17 层|0.50|≈ 53 人|

整体乘梯比例 ≈ (0×4 + 0.10×2 + 0.30×4 + 0.50×7) / 17 ≈ 0.27，接近 1/3。

#### 返程高峰（1 层 → 宿舍）

|楼层范围|用梯概率 P_use|说明|
|-|-|-|
|1-4 层|0|低楼层不乘梯|
|5-9 层|0.30|中间楼层部分乘梯|
|10-17 层|1.0|高层必然乘梯|

### 4.4 目标楼层分布

* 出行高峰：目标楼层固定为 1 层
* 返程高峰：目标楼层在可服务楼层中均匀分布

  * 单数电梯乘客：目标 ∈ {3, 5, 7, 9, 11, 13, 15, 17}，均匀分布
  * 双数电梯乘客：目标 ∈ {2, 4, 6, 8, 10, 12, 14, 16}，均匀分布
* 非高峰：双向均有，目标楼层在可服务楼层中均匀分布

---

## 5. 电梯调度算法（SCAN 策略）

### 5.1 核心规则

1. 同向响应：电梯沿当前方向运行时，响应所有同向的厅外呼叫和轿内请求
2. 反向延迟：反向呼叫在当前方向任务完成后才响应
3. 满员跳过：当 |passengers_e| = C_max 时，跳过所有中间楼层厅外呼叫（仅响应轿内请求对应楼层）
4. 方向反转：到达最高 / 最低请求楼层后，若无更高 / 更低请求，则反转方向或进入 IDLE

### 5.2 同组电梯互斥分配规则

同一组内的两台电梯（如 E_odd_1 与 E_odd_2）遵循以下规则：

* 当某楼层发出呼叫时，该呼叫仅分配给同组中一台电梯
* 分配策略：选择当前距离最近且空闲 / 同向的电梯；若距离相同，选择编号较小的
* 已接受请求的电梯在完成该请求前，同组另一台电梯不会再接收同一呼叫
* 若两台均忙，呼叫进入等待队列，待任一同组电梯空闲后分配

### 5.3 调度伪代码

**算法**：SCAN 电梯调度（含同组互斥）
**输入**：电梯状态集合 {state_e}, 厅外呼叫队列 hall_calls, 轿内请求 car_calls
**输出**：各电梯的下一动作

1. 新呼叫到达时：
a. 根据目标楼层奇偶性确定电梯组 G（单数组 or 双数组）
b. 在 G 中选择最优电梯 e*：

   * 优先选择 IDLE 状态的电梯
   * 若均非 IDLE，选择同向且距离最近的
   * 若距离相同，选编号较小者
c. 将呼叫分配给 e*，标记为 "已分配"
d. 同组另一台电梯不再接收此呼叫
2. 电梯 e 运行中：
a. 若 state_e = IDLE：

   * 检查已分配的厅外呼叫
   * 若有，设定方向，前往呼叫楼层
   * 若无，保持 IDLE
b. 若 state_e = MOVING：
   * 检查当前楼层是否有同向呼叫或轿内请求
   * 若有 且 |passengers_e| < C_max：停靠
   * 若 |passengers_e| = C_max：仅响应轿内请求楼层
   * 若前方无更多同向请求：反转方向
c. 停靠处理：
   * 开门（t_open 或 t_open_1）
   * 乘客下车（n_alight × t_alight）
   * 乘客上车（n_board × t_board，不超过 C_max）
   * 关门（t_close）

### 5.4 信息约束

电梯控制器仅知道：

* 自身当前位置 position_e
* 当前载客数 |passengers_e|
* 厅外呼叫按钮状态（哪些楼层按了上 / 下）
* 轿内目标按钮状态

电梯不知道：

* 各楼层实际等待人数
* 未来将产生的请求

---

## 6. 乘客行为模型

### 6.1 乘客状态机

每个乘客 p 的状态转移：ARRIVED（到达候梯层）→ WAITING（等待电梯）→ BOARDING（上车）→ RIDING（乘梯中）→ ALIGHTING（下车）→ DONE（完成）

### 6.2 候梯行为

* 乘客到达候梯层后，按下对应方向按钮
* 乘客选择电梯：根据目标楼层奇偶性选择对应电梯组

  * 1 层候梯：目标为单数层 → 等待单数组电梯；目标为双数层 → 等待双数组电梯
  * 其他楼层候梯：等待唯一对应组电梯
* 乘客不更换电梯（一旦选定等待）

### 6.3 放弃行为

* 最大等待时间 T_max_wait = 300s（5 分钟）
* 超时后乘客放弃乘梯，状态转为 ABANDONED，从等待队列移除

---

## 7. 仿真输出指标

|指标|符号|说明|
|-|-|-|
|平均等待时间|E[W]|从按下按钮到电梯到达的时间|
|最大等待时间|W_max|所有乘客中的最大等待时间|
|平均行程时间|E[T_trip]|从上车到下车的时间|
|平均完成时间|E[T_total]|从按下按钮到到达目标楼层|
|电梯利用率|U|电梯运行时间 / 总仿真时间|
|系统吞吐量|Q|单位时间内成功运送的乘客数|
|长等待比例|P(W > 120s)|等待超过 2 分钟的乘客比例|
|放弃率|P_abandon|因超时放弃乘梯的乘客比例|

---

## 8. 优化后的电梯调度算法（智能协作版）

### 8.1 现有算法分析与改进目标

#### 8.1.1 现有算法合理性分析

现有基于SCAN算法的调度方案具有以下优势：

| 优势点 | 说明 | 合理性依据 |
|--------|------|------------|
| SCAN算法实现正确 | 经典电梯调度算法，响应式调度模式 | 符合7号楼实际电梯运行方式 |
| 分组服务策略清晰 | 奇数/偶数层分离，避免交叉干扰 | 与建筑实际配置一致 |
| 同组互斥机制有效 | 防止重复分配，保证响应一致性 | 系统资源约束条件下的必要机制 |
| 模块化设计规范 | 便于扩展和维护 | 软件工程良好实践 |

同时存在以下可改进之处：

| 问题类别 | 具体表现 | 影响程度 |
|----------|----------|----------|
| 即时分配局限 | 新呼叫到达立即分配，不考虑后续可能 | 中 |
| 缺乏全局视野 | 无任务缓存机制，无法批量优化 | 高 |
| 双电梯协作不足 | 仅停留在互斥层面，无主动协作 | 中高 |
| 无预测能力 | 无法提前部署电梯 | 中 |

#### 8.1.2 改进目标

本次优化的目标：

| 目标 | 量化指标 | 预期改进幅度 |
|------|----------|--------------|
| 平均等待时间降低 | E[W] < 35s | > 20% |
| 长等待比例降低 | P(W > 120s) < 5% | > 50% |
| 电梯利用率平衡 | 同组电梯利用率差异 < 10% | > 30% |
| 系统吞吐量提升 | Q提升 | > 15% |

### 8.2 优化算法的形式化描述

#### 8.2.1 符号定义与系统状态

定义系统状态向量 Σ(t) 如下：

$$\Sigma(t) = \left\langle \mathcal{E}(t), \mathcal{H}(t), \mathcal{P}(t), \mathcal{T}(t) \right\rangle$$

其中：

| 组件 | 符号 | 含义 |
|------|------|------|
| 电梯状态集合 | $\mathcal{E}(t)$ | $\{E_i(t) \mid i=1,2,3,4\}$，各电梯状态 |
| 厅外呼叫集合 | $\mathcal{H}(t)$ | $\{H_f^d(t) \mid f \in \mathcal{F}, d \in \{UP,DOWN\}\}$ |
| 乘客状态集合 | $\mathcal{P}(t)$ | $\{P_j(t) \mid j=1,2,\dots,N_{total}\}$ |
| 任务仓库状态 | $\mathcal{T}(t)$ | $\langle \mathcal{Q}_w(t), \mathcal{Q}_a(t), \mathcal{Q}_c(t), \mathcal{H}_d(t) \rangle$ |

单台电梯状态定义：

$$E_i(t) = \left\langle pos_i(t), dir_i(t), load_i(t), state_i(t), \mathcal{C}_i(t) \right\rangle$$

其中：

| 变量 | 取值范围 | 含义 |
|------|----------|------|
| $pos_i(t)$ | $\{1,2,\dots,17\}$ | 当前楼层位置 |
| $dir_i(t)$ | $\{UP, DOWN, IDLE\}$ | 当前运行方向 |
| $load_i(t)$ | $\{0,1,\dots,12\}$ | 当前载客数 |
| $state_i(t)$ | $\{MOVING, DOOR_OPEN, IDLE\}$ | 运行状态 |
| $\mathcal{C}_i(t)$ | $\subseteq \mathcal{F}$ | 当前轿内请求楼层集合 |

#### 8.2.2 任务仓库机制

任务仓库是优化方案的核心组件，定义为四元组：

$$\mathcal{T}(t) = \left\langle \mathcal{Q}_w(t), \mathcal{Q}_a(t), \mathcal{Q}_c(t), \mathcal{H}_d(t) \right\rangle$$

其中：

| 队列 | 类型 | 排序规则 | 功能说明 |
|------|------|----------|----------|
| $\mathcal{Q}_w(t)$ | 优先队列 | 按优先级Score降序 | 待分配厅外呼叫 |
| $\mathcal{Q}_a(t)$ | 哈希表 | 按电梯ID索引 | 已分配任务记录 |
| $\mathcal{Q}_c(t)$ | 循环缓冲区 | 按时间顺序 | 已完成任务历史 |
| $\mathcal{H}_d(t)$ | 时间序列 | 按时段索引 | 历史流量数据 |

#### 8.2.3 优先级评分函数

对每个待分配的呼叫 $h = (f, d, t_{req}, p)$，计算优先级评分：

$$Score(h, t, \Sigma) = \alpha \cdot W(h, t) + \beta \cdot D(h, \Sigma) + \gamma \cdot L(\Sigma) + \delta \cdot Dir(h, \Sigma) + \varepsilon \cdot Pred(h, t)$$

各分量定义：

| 分量 | 符号 | 计算方式 | 取值范围 |
|------|------|----------|----------|
| 等待时间 | $W(h, t)$ | $(t - t_{req}) / T_{max_wait}$ | $[0, 1]$ |
| 距离因子 | $D(h, \Sigma)$ | $1 - \min_i \{ |pos_i(t) - f| / N_{floor}\}$ | $[0, 1]$ |
| 负载均衡 | $L(\Sigma)$ | $1 - (load_i(t) / C_{max})$，针对最优电梯 $i$ | $[0, 1]$ |
| 方向一致性 | $Dir(h, \Sigma)$ | $1$ 若同向，$0.5$ 若IDLE，$0$ 若反向 | $[0, 1]$ |
| 预测价值 | $Pred(h, t)$ | $f(f, d, period(t))$，基于历史统计 | $[0, 1]$ |

权重系数 $\alpha, \beta, \gamma, \delta, \varepsilon$ 为可配置参数，默认值：

$$\alpha = 0.3, \quad \beta = 0.25, \quad \gamma = 0.2, \quad \delta = 0.15, \quad \varepsilon = 0.1$$

系数动态调整策略：

$$w_{period} = \begin{cases}
(\alpha=0.35, \beta=0.2, \gamma=0.25, \delta=0.15, \varepsilon=0.05), & \text{高峰期} \\
(\alpha=0.25, \beta=0.3, \gamma=0.15, \delta=0.2, \varepsilon=0.1), & \text{非高峰期}
\end{cases}$$

#### 8.2.4 智能分配算法

智能分配算法是优化的核心，定义如下：

**算法**：智能任务分配器

**输入**：
- 系统状态 $\Sigma(t)$
- 待分配任务队列 $\mathcal{Q}_w(t)$
- 触发条件：新任务到达 $\lor$ 电梯状态变化 $\lor$ 周期触发

**输出**：
- 更新后的分配状态 $\mathcal{Q}_a(t)$

**伪代码**：

```
FUNCTION SmartAssigner(Σ(t), Qw(t)):
    # 步骤1：评估候选电梯
    FOR EACH call h IN Qw(t):
        # 确定候选电梯组（奇数/偶数层）
        G = DetermineGroup(h)
        
        # 对每个候选电梯计算评分
        FOR EACH elevator i IN G:
            score_ij = ComputeScore(h, i, Σ(t))
        
        # 选择最优电梯
        i* = argmax_i {score_ij}
        
        # 预评估：是否导致系统整体效用下降
        IF PreEvaluate(i*, h, Σ(t)) > threshold:
            Assign(i*, h)
            MoveFromWaitToAssigned(h, i*)
    
    # 步骤2：批量重分配（仅周期触发）
    IF trigger_type == periodic:
        FOR EACH i IN {1,2,3,4}:
            IF LoadImbalance(i, Σ(t)) > threshold:
                ReassignTasks(i, Σ(t))
    
    # 步骤3：预调度（空闲电梯）
    FOR EACH i IN {1,2,3,4}:
        IF state_i(t) == IDLE AND Qa(t)[i] == empty:
            pred_pos = PredictHotSpot(t, Hd(t))
            MoveToPredictedPosition(i, pred_pos)

RETURN updated Qa(t)
```

#### 8.2.5 双电梯协作机制

同组两台电梯（如 $E_{odd1}$ 和 $E_{odd2}$）之间的协作形式化描述：

定义协作状态函数 $\Phi(i_1, i_2, t)$：

$$\Phi(i_1, i_2, t) = \begin{cases}
"separated", & |pos_{i1}(t) - pos_{i2}(t)| > k \land dir_{i1}(t) \neq dir_{i2}(t) \\
"following", & |pos_{i1}(t) - pos_{i2}(t)| \leq k \land dir_{i1}(t) = dir_{i2}(t) \\
"converging", & |pos_{i1}(t) - pos_{i2}(t)| > k \land dir_{i1}(t) = dir_{i2}(t)
\end{cases}$$

其中阈值 $k = 4$（约4层楼的距离）。

协作策略：

$$Policy(Φ) = \begin{cases}
独立分区服务, & Φ = "separated" \\
接力式服务, & Φ = "following" \\
负载均衡重分配, & Φ = "converging" \land load_{imbalance} > threshold
\end{cases}$$

#### 8.2.6 优化后的电梯运行策略

在SCAN算法基础上，增加以下优化策略：

1. **批量停靠优化**：当同层有多个上下车乘客时，一次性处理，避免重复停靠
2. **顺路性判断**：若呼叫在电梯当前路径上，优先响应
3. **ETA预估**：基于当前任务队列预估电梯到达时间

$$ETA(i, f) = T_{travel} \cdot |pos_i(t) - f| + \sum_{j \in \text{path}_i(t)} T_{stop}(j)$$

其中 $T_{stop}(j)$ 为楼层 $j$ 的预计停靠时间。

#### 8.2.7 预测与预调度

基于历史数据的预测模型：

$$Pred(f, d, t) = \mu_{f,d}(period(t)) + \epsilon_{f,d}(t)$$

其中 $\mu_{f,d}(period(t))$ 是时段 $period(t)$ 内 $(f, d)$ 方向呼叫的历史平均值，$\epsilon_{f,d}(t)$ 为修正项。

预调度策略：

$$i_{pred} = \argmin_i \left\{ |pos_i(t) - argmax_f Pred(f, d, t)| \right\}$$

### 8.3 改进后的仿真输出指标扩展

在原有指标基础上，增加以下评估指标：

| 新增指标 | 符号 | 说明 |
|----------|------|------|
| 同组负载不平衡度 | $U_{diff}$ | $|U_1 - U_2|$，反映协作效率 |
| 任务重分配次数 | $N_{reassign}$ | 优化算法的动态调整频率 |
| 批量停靠比例 | $P_{batch}$ | 一次停靠处理多层请求的比例 |
| 预调度命中次数 | $N_{pred_hit}$ | 预测准确的次数 |

---

## 9. 两种算法的对比框架

### 9.1 架构对比

| 维度 | 原算法 | 优化算法 |
|------|--------|----------|
| 呼叫分配模式 | 即时分配 | 任务仓库 + 智能分配 |
| 电梯间关系 | 互斥为主 | 深度协作 |
| 决策视野 | 单步响应 | 全局优化 |
| 预测能力 | 无 | 有 |

### 9.2 实验设计

建议采用A/B测试框架进行对比：

$$\Delta_{metric} = Metric_{optimized} - Metric_{original}$$

对所有指标量化对比，评估改进效果。
